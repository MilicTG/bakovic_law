import dynamicHeader from "@/sanity/schemas/header-schema";
import aboutUsSchema from "@/sanity/schemas/about-schema";
import lawCategorySchema from "@/sanity/schemas/law-category-schema";

const schemas = [dynamicHeader, aboutUsSchema, lawCategorySchema]

export default schemas